server:
  port: 8080
  tomcat:
    uri-encoding: UTF-8
  servlet:
    context-path: /myBoot
spring:
  http:
    encoding:
      charset: UTF-8
      enabled: true
      force: true
  messages:
    encoding: UTF-8
  profiles:
    active: dev
  banner:
    charset: UTF-8